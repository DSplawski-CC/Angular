import { BlockWheelingDirective } from './block-wheeling.directive';

describe('BlockWheelingDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockWheelingDirective();
    expect(directive).toBeTruthy();
  });
});
